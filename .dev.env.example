# Development. Copy to .dev.env.
# Run: docker compose --env-file .dev.env -f compose.dev.yml up --build

AUTH_MODE=manual
PUBLIC_URL=http://localhost:4100
HOST=0.0.0.0
PORT=4100
DB_PATH=/data/mediation.db
SESSION_TTL_MS=120000
